-r requirements.txt
# Herramientas de prueba, fijadas igual que el runtime (E1-037).
pytest==9.1.1
httpx2==2.10.0
# Usada por las pruebas que validan el flujo de integración continua.
PyYAML==6.0.3
# Validación de sintaxis del CSS en las pruebas de la capa móvil.
tinycss2==1.5.1
# E4-030 — guardas de CI: auditoría de dependencias vulnerables y de secretos.
pip-audit==2.10.1
detect-secrets==1.5.0
